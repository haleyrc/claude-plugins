# templ

The conventions in this document apply to working with `templ` template files and the Go code generated from those files.

## Operating Principles

You do not need to attempt to format templ code directly; instead run `templ fmt PATH_TO_FILE` after editing.
